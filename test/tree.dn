module tree;
int bar;
int func(int x, int y) {
  ;
  quack(x, y);
  x+y();
  if (x) {}
  {
    int x = 4+5*a, y=5, z;
  }
  if(abc) yz=5;
  else q=z;
  /*  $@*!&$!@)(*&!$@)($*@&!)($*@&@(!)$*) */
  // $(#*@^$!(*!$&@)(*$@!&()!@$*&(@$)&(%*#!&%#(@))))
  while(foo) { //bar
    bar=baz+5;
  } else {
    baz=bar+5;
  }
}
